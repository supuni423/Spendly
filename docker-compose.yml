services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: spendly
      POSTGRES_PASSWORD: spendly
      POSTGRES_DB: spendly
    ports:
      - "5432:5432"
    volumes:
      - spendly_pgdata:/var/lib/postgresql/data

volumes:
  spendly_pgdata:
